{
  "name": "mastergo-api-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MasterGo plugin bridge that connects to mcp-server over WebSocket.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "packageManager": "yarn@4.9.1",
  "devDependencies": {
    "@mastergo/plugin-typings": "^2.18.4",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
